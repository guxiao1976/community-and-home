# Request: 工作记录模块开发 v2（流水线修复后）

**用户原话**: 
```
利用修复后的流水线，重新开发工作记录模块。

功能需求：
在 web pc 前端增加一个一级菜单"AI-Coding自我提升"，下面有两个页面：

第一个页面是"工作事项维护"：
- 只有一列，维护工作事项
- 实现增删改查功能

第二个页面是"工作内容记录"：
- 4个列：工作事项（下拉列表，选择前面页面维护的事项）、工作日期、工作内容描述、工作时长
- 查询条件是日期

技术要求：
- 这两个页面都归属到主数据微服务下面
- 包括前后端、数据模型等完整实现
- 数据库表、相关模块前加标记前缀 `pipeline_test_` 与实际项目区分
- 充分记录日志，为查找开发流水线的问题提供数据
```

**路径**: OpenSpec

**理由**: 
- 跨服务（web/pc 前端 + master-data-service 后端）
- 新功能开发
- 可能涉及 Proto 变更

**涉及服务**:
- `services/master-data-service` - 主数据服务（后端 API+RPC）
- `web/pc` - PC 管理后台前端
- `api-proto/` - Proto 接口定义（如需新增 RPC）

**创建时间**: 2026-06-23 06:15

**与 v1 的区别**:
- v1: 测试流水线，发现 P0 问题
- v2: 验证 P0 修复，完成实际开发

**特殊要求**:
1. 验证 P0-1 修复：检查 Workflow 日志中的 args 参数调试信息
2. 验证 P0-2 修复：如遇工具失败，观察是否触发熔断机制
3. 复用 v1 的设计文档（design.md, tasks.md）
