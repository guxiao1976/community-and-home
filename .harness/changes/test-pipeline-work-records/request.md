# Request: 测试流水线-工作记录模块

**用户原话**: 
```
流水线已经初步打造好了，需要在实践中提升，完成下面需求：
在web pc 前端增加一个一级菜单 AI-Coding自我提升，下面有两个页面，
第一个页面是"工作事项维护"，只有一列，维护工作事项，实现增删改查。
第二个页面是"工作内容记录"页面：有4个列，工作事项（下拉列表，选择前面页面维护的事项），
工作日期，工作内容描述，工作时长，查询条件是日期。
这两个页面都归属到主数据微服务下面，你来开发所有内容，包括不限于前后端、数据模型等，
这个模块主要是为了测试开发流水线的，可以在数据库表、相关模块前加标记前缀与实际项目区分。
我希望开发过程中充分记录日志，为查找开发流水线的问题提供数据，
开发完成后，对本次开发流水线、开发过程进行评价，找出流水线需要提升的点。
```

**路径**: OpenSpec

**理由**: 
- 跨服务开发（web/pc 前端 + master-data-service 后端）
- 新功能开发，需要完整的需求分析和架构设计
- 可能涉及 Proto 变更（新增 RPC 接口）

**涉及服务**:
- `services/master-data-service` - 主数据服务（后端 API+RPC）
- `web/pc` - PC 管理后台前端
- `api-proto/` - Proto 接口定义（如需新增 RPC）

**创建时间**: 2026-06-22 20:50

**特殊要求**:
1. 数据库表、模块前加标记前缀（如 `test_` 或 `pipeline_test_`）与实际项目区分
2. 充分记录日志，为流水线问题排查提供数据
3. 开发完成后评价开发流水线，找出提升点

**功能概述**:
- 菜单：AI-Coding自我提升（一级菜单）
- 页面1：工作事项维护（单列表，增删改查）
- 页面2：工作内容记录（4列：工作事项下拉、日期、描述、时长，按日期查询）
