---
id: task-2026-08-15-008
title: "harness-pipeline 执行评审无 CRITICAL 一票否决"
service: harness
type: debt
priority: P2
status: closed
source: human
source_detail: "spec-pipeline 需求评审（stage2）已加 CRITICAL 一票否决（mustFixes severity=critical），但 harness-pipeline 执行评审（review.js 的 3 lens 投票）仍是 2/3 PASS 就通过，CRITICAL 被放行。实测 web/mobile 字段对齐前，design-biz 视角报 snake/camel 不匹配 CRITICAL，但 2/3 PASS 放行（pipeline verdict=pass），靠 Owner 事后发现并补修。建议：执行评审对齐需求评审，CRITICAL（severity=critical）一票否决，或至少把 CRITICAL 显式上报 Owner。"
created: 2026-08-15
blocks: []
blocked_by: []
assigned_run: ""
completed: "2026-08-15"
outcome: ""
triage: ""
workload: "M"
dispatch_count: 0
---

# harness-pipeline 执行评审无 CRITICAL 一票否决

## 问题

spec-pipeline 需求评审（stage2）已加 CRITICAL 一票否决（mustFixes severity=critical），但 harness-pipeline 执行评审（review.js 的 3 lens 投票）仍是 2/3 PASS 就通过，CRITICAL 被放行。实测 web/mobile 字段对齐前，design-biz 视角报 snake/camel 不匹配 CRITICAL，但 2/3 PASS 放行（pipeline verdict=pass），靠 Owner 事后发现并补修。建议：执行评审对齐需求评审，CRITICAL（severity=critical）一票否决，或至少把 CRITICAL 显式上报 Owner。

## 完成标准

- [ ] （待定义）

## 执行记录

| 日期 | 事件 | 详情 |
|------|------|------|
| 2026-08-15 | 创建 | spec-pipeline 需求评审（stage2）已加 CRITICAL 一票否决（mustFixes severity=critical），但 harness-pipeline 执行评审（review.js 的 3 lens 投票）仍是 2/3 PASS 就通过，CRITICAL 被放行。实测 web/mobile 字段对齐前，design-biz 视角报 snake/camel 不匹配 CRITICAL，但 2/3 PASS 放行（pipeline verdict=pass），靠 Owner 事后发现并补修。建议：执行评审对齐需求评审，CRITICAL（severity=critical）一票否决，或至少把 CRITICAL 显式上报 Owner。 |
| 2026-08-15 | 关闭 | open → closed |

## 关联

- 来源: spec-pipeline 需求评审（stage2）已加 CRITICAL 一票否决（mustFixes severity=critical），但 harness-pipeline 执行评审（review.js 的 3 lens 投票）仍是 2/3 PASS 就通过，CRITICAL 被放行。实测 web/mobile 字段对齐前，design-biz 视角报 snake/camel 不匹配 CRITICAL，但 2/3 PASS 放行（pipeline verdict=pass），靠 Owner 事后发现并补修。建议：执行评审对齐需求评审，CRITICAL（severity=critical）一票否决，或至少把 CRITICAL 显式上报 Owner。
