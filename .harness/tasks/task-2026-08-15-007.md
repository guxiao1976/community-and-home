---
id: task-2026-08-15-007
title: "spec-pipeline 评审报告/归档落盘在沙箱环境失效（fs=null）"
service: harness
type: debt
priority: P2
status: closed
source: human
source_detail: "spec-pipeline.js:24-25 用 try { fs=require('fs') } catch 降级，沙箱环境 fs=null，导致 if(fs) 保护的落盘（spec_review_*.md / design_review_*.md / summary.md / INDEX 更新 / pipeline-state.json）全部跳过。proposal/spec/tasks/design 能落盘是因为 agent 用 Write 工具（完整环境），但 workflow 自身的 fs.writeFileSync 落盘失效。实测 rel-user-role-migration-publish-fix 跑完：review/ 目录空、summary.md 未生成、INDEX 未更新，靠 Owner 手动补归档。建议：评审报告/归档改由 agent 的 Write 工具落盘，或 workflow 落盘逻辑移出沙箱。"
created: 2026-08-15
blocks: []
blocked_by: []
assigned_run: ""
completed: "2026-08-15"
outcome: ""
triage: ""
workload: "M"
dispatch_count: 0
---

# spec-pipeline 评审报告/归档落盘在沙箱环境失效（fs=null）

## 问题

spec-pipeline.js:24-25 用 try { fs=require('fs') } catch 降级，沙箱环境 fs=null，导致 if(fs) 保护的落盘（spec_review_*.md / design_review_*.md / summary.md / INDEX 更新 / pipeline-state.json）全部跳过。proposal/spec/tasks/design 能落盘是因为 agent 用 Write 工具（完整环境），但 workflow 自身的 fs.writeFileSync 落盘失效。实测 rel-user-role-migration-publish-fix 跑完：review/ 目录空、summary.md 未生成、INDEX 未更新，靠 Owner 手动补归档。建议：评审报告/归档改由 agent 的 Write 工具落盘，或 workflow 落盘逻辑移出沙箱。

## 完成标准

- [ ] （待定义）

## 执行记录

| 日期 | 事件 | 详情 |
|------|------|------|
| 2026-08-15 | 创建 | spec-pipeline.js:24-25 用 try { fs=require('fs') } catch 降级，沙箱环境 fs=null，导致 if(fs) 保护的落盘（spec_review_*.md / design_review_*.md / summary.md / INDEX 更新 / pipeline-state.json）全部跳过。proposal/spec/tasks/design 能落盘是因为 agent 用 Write 工具（完整环境），但 workflow 自身的 fs.writeFileSync 落盘失效。实测 rel-user-role-migration-publish-fix 跑完：review/ 目录空、summary.md 未生成、INDEX 未更新，靠 Owner 手动补归档。建议：评审报告/归档改由 agent 的 Write 工具落盘，或 workflow 落盘逻辑移出沙箱。 |
| 2026-08-15 | 关闭 | open → closed |

## 关联

- 来源: spec-pipeline.js:24-25 用 try { fs=require('fs') } catch 降级，沙箱环境 fs=null，导致 if(fs) 保护的落盘（spec_review_*.md / design_review_*.md / summary.md / INDEX 更新 / pipeline-state.json）全部跳过。proposal/spec/tasks/design 能落盘是因为 agent 用 Write 工具（完整环境），但 workflow 自身的 fs.writeFileSync 落盘失效。实测 rel-user-role-migration-publish-fix 跑完：review/ 目录空、summary.md 未生成、INDEX 未更新，靠 Owner 手动补归档。建议：评审报告/归档改由 agent 的 Write 工具落盘，或 workflow 落盘逻辑移出沙箱。
