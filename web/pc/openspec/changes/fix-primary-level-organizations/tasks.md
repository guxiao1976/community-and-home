## 1. 重构页面为双 Tab 结构

- [x] 1.1 在 `views/grassroots/Index.vue` 中引入 `el-tabs` 和 `el-tab-pane`，创建"查询编辑"和"提交管理"两个 Tab
- [x] 1.2 将现有筛选栏（级联选择器 + 搜索按钮 + 新增按钮）和树形表格移入"查询编辑"Tab
- [x] 1.3 从"查询编辑"Tab 的表格操作列中移除"提交"按钮

## 2. 查询编辑 Tab（保持现有功能）

- [x] 2.1 保留省-市-区县级联选择器和搜索按钮
- [x] 2.2 保留状态筛选作为可选辅助过滤（与区县条件组合使用）
- [x] 2.3 保留树形表格（街道→社区懒加载）和编辑、添加下级、删除操作
- [x] 2.4 保留新增街道/乡镇按钮和对话框

## 3. 提交管理 Tab — 状态筛选

- [x] 3.1 创建提交管理 Tab 的状态筛选栏：全部 / 待提交 / 已提交 / 已批准 / 已拒绝 / 待删除（el-radio-group）
- [x] 3.2 默认选中"待提交"状态，切换状态时自动查询

## 4. 提交管理 Tab — 平铺表格

- [x] 4.1 创建平铺表格（非树形），列：选择框、区划代码、区划名称、完整路径、级别、提交状态、操作
- [x] 4.2 实现数据查询：调用 `getAdministrativeDivisions({ level: [4,5], submission_status, page, page_size })` 加载数据
- [x] 4.3 实现 ID→名称路径解析：首次加载时获取 level 1-3 区划数据构建 `Map<id, name>`，将 path 字段中的 ID 替换为名称，用 " > " 连接
- [x] 4.4 实现分页：支持分页查询和翻页

## 5. 提交管理 Tab — 提交操作

- [x] 5.1 实现单条提交：操作列"提交"按钮，仅 submission_status 为 0 或 3 时显示
- [x] 5.2 实现批量提交：勾选多条后显示"批量提交"按钮，调用 `batchSubmitDivisions({ ids })`
- [x] 5.3 选择框仅在可提交行（status 0 或 3）上显示，已提交/已批准行不可勾选
- [x] 5.4 提交成功后刷新表格数据

## 6. 验证

- [x] 6.1 验证查询编辑 Tab：区县定位、状态筛选组合、CRUD 操作正常
- [x] 6.2 验证提交管理 Tab：状态筛选、完整路径显示、单条/批量提交正常
- [x] 6.3 验证 Tab 切换：切换后各 Tab 数据和筛选状态保留
