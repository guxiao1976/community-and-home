# 开发日志 — 2026-08-11

## 今日概览

- **涉及服务**：user-service, permission-service, auth-service, web/pc, web/mobile
- **工作类型**：架构重构（RBAC 角色体系合并）+ 功能开发（认证 API）+ 文档整理

## 关键变更

### [全局] 时间字段命名标准化
- **做了什么**：27 张表列名 + 35+ Go 文件 + 前端 TS/Vue 统一为 `created_at`/`updated_at`/`deleted_at`
- **为什么**：Proto 用 `_at`，DB/Go/前端用 `_time`，全链路不一致
- **影响**：编码规范新增 §5.1 时间字段命名、§5.2 雪花 ID 列宽；QA 检查 `check-api-field-align.sh` 支持
- **关联**：提交 `698edc3` 之前的专项

### [全局] RBAC 角色体系合并（方案 B）
- **做了什么**：
  - `rel_user_role` 扩展 status/verified_at/expires_at（个体角色生命周期）
  - 角色从 4 个扩到 8 个（+tenant/committee/merchant/sys_admin）
  - 废弃 `user_membership_role`，认证流程迁移到 permission-service
  - auth-service JWT 角色来源切换（经 user-service 代理）
- **为什么**：两套角色体系（JWT 用 user_membership_role，前端用 rel_user_role）互不相通
- **影响**：user-service 认证流程、permission-service 角色权威、auth JWT、前端 PC/移动端
- **踩坑**：roleMapper 全局缓存导致测试串扰，需 `resetRoleMapper()`
- **提交**：待提交

### [user-service] 补认证 REST API
- **做了什么**：新增 POST/GET `/api/users/certifications`、GET `/api/verifications`、POST `/api/verifications/:id/review`
- **为什么**：前端审核中心有页面但无后端接口，认证流程无法端到端走通
- **影响**：移动端提交认证、PC 审核中心可用

### [全局] docs/specs 目录整理
- **做了什么**：归档 15 个过时文档到 `docs/archived/specs/`，新增 INDEX.md
- **关联**：提交 `4414e12`

## 遗留问题

- [ ] 角色合并代码尚未提交（待整理后提交）
- [ ] 移动端认证提交 UI 待接后端新路由验证
- [ ] 认证 REST API 的管理员权限校验待确认

## 明日计划

- [ ] 提交角色合并全部代码
- [ ] 建立开发日志机制（docs/devlog/ + 补记历史）
