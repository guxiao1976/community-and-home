# 开发日志 — 2026-08-14

## 今日概览

- **工作类型**：构建并验证「规范驱动全流程自动化流水线」（harness-spec-pipeline.js）+ 文档一致性收尾 + SDD 需求分析增强 + 评估 skill 同步 + harness 自检机制 + 整体架构总纲 + **全面评审 + 三梯队质量修复 + 清理精简**
- **核心成果**：全流程 0→6 真实 L 级端到端跑通、修复 6+ bug、设计文档与代码一致化、需求分析落地 5W2H/MoSCoW/异常清单、harness 自检（meta-CI）从 5 项扩到 8 项、**harness "声明≠实际"漂移系统收敛**（评审三梯队修复 20+ commit）、`.harness/` 清理 307M→4.7M

## 关键变更

### [Harness] spec-pipeline 全流程自动化建设

- **动机**：用户期望「从输入到归档的全流程自动化」（规范驱动），现状只自动化了编码+测试（harness-pipeline.js）
- **实现**：新建 harness-spec-pipeline.js 编排阶段 0-6（dispatch→需求分析→评审→架构→Proto→编码→归档），每阶段末 HITL 暂停等用户
- **沙箱限制**：Workflow 沙箱无 fs/child_process/本地 require → resume 状态经 args.resumeState 传 ctx，文件级门禁由子 Agent 完整环境承担
- **门禁**：内置纯逻辑门禁（评审投票/轮次）+ 信任 agent 报告（traceability/tasksCount）

### [验证] 真实 L 级端到端 0→6（角色列表排序需求）

- 0 dispatch(L) → 1 澄清 10 决策 → 2 评审(3轮 escalate→放宽) → 3 架构(10 tasks) → 4 Proto(真实 make ci) → 5 编码(harness-pipeline 一次 PASS, confidence 1.0) → 6 归档(pass)
- 全程 6 次 HITL 暂停用户拍板
- **真实交付**：permission-service 排序实现（FindList 签名扩展 + validateSort 白名单 + API 透传 + ToError 修复，~151 测试全绿）+ api-proto SortField

### [修复] 端到端暴露的 6+ bug

1. 沙箱无 fs → resume 改 args.resumeState（原磁盘 state 写不了）
2. Date.now/new Date 禁用 → state 去时间戳
3. gate-engine 沙箱 require 失败 → 内置门禁 + 信任 agent 报告
4. proto_done 决策未映射 → 阶段 4 门禁误 FAIL
5. svcName 对象三种形态不兼容 → 阶段 6 崩溃
6. qa.js 模板字符串反引号 → harness-pipeline.js 解析失败

### [文档] 设计文档与自动化流程一致性收尾

- **pipeline-flow-complete.md** 重写为「Harness Spec Pipeline 完整流程图」（spec-pipeline 自动编排 0-6 + 6 HITL 暂停点 + resume + 失败路由），替换旧人工编排图
- **pipeline-architecture.md** 补「双层流水线总览」统一命名：spec-pipeline（全流程）vs harness-pipeline（编码）
- **pipeline-patterns.md** §10.4 补全流程自动化模式（HITL/resume/门禁分层/编码委托）
- **pipeline-evolution.md** 追加 Phase 18「spec-pipeline 全流程自动化」
- **评估/工具 skill 同步**：pipeline-review（流水线自评）正向测试改走 spec-pipeline + 文档清单补全；select-tool（工具选择）L/S/M 路由改 spec-pipeline 移除旧「派发子 Agent 序列」；dispatch 路由摘要表同步——全仓 skill 无残留旧流程引用
- **owner-agent 调度结构同步**：阶段表从「Owner 派发子 Agent / N×Workflow 并行」（人工编排）改为「spec-pipeline 自动编排 + 6 个 HITL 决策点 + 2 个 Owner 内联（make ci/编码委托）」——明确「Owner 管边界、workflow 管编排」的分层调度
- **消除重复分级**：阶段 0 原实现总是自己调 agent 判定 S/M/L，与 Owner 入口判定重复 → 改为优先消费 `args.workload`（Owner 入口判定传入），仅缺失时兜底判定
- **harness 整体架构总纲**：新建 `harness-architecture.md`（6 大子系统：开发流水线/知识管理/上下文管理/门禁体系/任务变更/自检机制 + 协作 + 目录），替代 pipeline-architecture 作为架构入口（降级为开发流水线子系统详参）——解决「harness 架构知识散落」缺口
- **自检机制（meta-CI）**：新建 `harness-self-check.sh`（流程引用/命名口径/文档同步/配置漂移/调用链 5 项），接入 pre-commit（改动 harness 自动跑，FAIL 阻止提交）+ pipeline-review 维度1——解决「改了这里、那边不一致」无自动化兜底；首跑即抓到并修复 select-tool 残留旧词

### [需求分析] SDD 方法论落地（5W2H / 不做清单 / 异常场景穷举）

- **动机**：用户指出 Brainstorming 盲区——需求挖不透（隐性需求遗漏）、范围失控（越做越多）、异常场景漏（后期返工）
- **调研**：OpenSpec Explore 定位 = Superpowers Brainstorming 上游前置；评估后不引入外部 skill（requirement-analyst 已承担探索职责），吸收方法论进 requirement-analysis.md
- **落地**：
  - Step 1 澄清加 **5W2H 7 维补全**（Who/What/When/Where/Why/How/How much，L 新功能完整、S/M 精简）+ **用户故事地图**（复杂业务可选）
  - Step 5 proposal 加 **不做清单**（MoSCoW Won't have，防范围蔓延）
  - Step 6 spec 加 **异常场景穷举**（边界输入/系统异常/权限/并发四类），与变异测试闭环
- **不引入**：OpenSpec Explore 外部 skill、事件风暴、第二套门控 Skill（均重叠/过重）

### [Harness] 全面评审 + 三梯队质量修复（声明≠实际 系统收敛）

**动机**：用户要求按 harness 思想全面评审流水线质量，聚焦「高质量自动化开发流水线建设」

**评审方法**：4 个并行评审 agent 按 harness-architecture 的 6 大子系统（流水线/门禁/知识/上下文/任务变更/自检）产出发现清单 → **逐项代码验证甄别**（关键：不盲信 agent，区分真实 bug / 文档过时 / 误报）

**第一梯队（高风险）**：
- **AST 检查死锁**：清理时删 go-ast-checker 二进制，但 harness-checks 只在二进制存在时才调 ast-checks.sh → 移除前置条件，AST 真执行（json_string 从 regex 降级恢复为 AST 校验）
- **P1 自动派发死代码**：auto_dispatch 主循环遍历 `p0_auto`，`combined`(P0+P1) 白构建 → 改为遍历 combined，P1 限量派发生效（传感器任务不再只进不出）
- **install-hooks 破坏 pre-commit**：cp 安装 vs pre-commit 的 `readlink+../../..` 假设 symlink → pre-commit 改 `git rev-parse --show-toplevel`（与 post-commit 一致，兼容两种安装）
- **changes/INDEX 补全**：13 变更目录仅登记 4 个 + ai-model-enhancements 悬空链接 → 全覆盖 + 修悬空；自检加第 7 项追溯链完整性（防再漂移）
- **孤儿 P0 任务**：`P0-*.md` 不匹配 `task-*.md` glob 永不被派发 → 归档（已实质完成）

**第二梯队**：
- **prompt 双轨收敛**：templates/*-new.js + build-pipeline-new.sh 契约与 core 断裂且 0 调用点 → 归档 `docs/_archive/prompts-new/`，回归单一 prompt 源（消除误跑生成坏流水线风险）
- **spec-pipeline 阶段机**：S/M 短路 `ctx.services` 为空导致无法派发编码 → ARGS_WORKLOAD 分支读 args.services；阶段 6 off-by-one / 阶段 4 不重试经验证为误报（主循环 stage_fail 时 return 不 +1，currentStage 是 resume 起点）
- **记忆体检接入**：knowledge-maintain.sh --check 从未被调用 → 自检加第 8 项 memory_health；修复 check 引号 bug（--check 误入双引号，体检从未真跑——防回归测试暴露）
- **graph-populator 去硬编码**：infra.go 硬编码服务清单缺 monitoring-service + community-hub module 错 → `discoverServices()` 扫描 services/*/go.mod 自动发现；pre-commit 支持 go.work 外模块（GOWORK=off go -C）

**第三梯队**：
- gate-engine verify 接线标注修正（yml banner 声称 verify×5 已落地，实为死代码）
- QA checkCount 从硬编码 6/14 改为从脚本派生（18/7）
- L1 上下文预算统一（owner-agent ~800/≤600/knowledge-load 400 三处矛盾 → 实际 734/预算 400）
- QA 项数 15→18 同步（dispatch/owner-agent）

**自主收尾（小而真实）**：
- knowledge-load 多关键词加分累加（MEM_SCORES 覆盖 bug → 命中词数正确累加）
- cross-service import 补 /internal 盲区（此前只匹配 /model）
- 归档 4 个死脚本（harness-tasks-debug/score/deps + run-all-checks）+ setup-cron 每周检查改 harness-self-check
- submodule 图谱产物提交（3 子仓库 + gitlink）+ **graph-sync 幂等写**（graph-context 时间戳每次 sync 变化导致"提交→sync→再提交"循环，除时间戳外一致则跳过写）

**清理精简**：`.harness/` 307M→4.7M、顶层目录 20→14、新增 `.harness/README.md` 目录说明（14 目录职责 + 边界约定 + 清理规则 + skills 自建/第三方清单）、服务名映射外提 registry 单一数据源

**经验沉淀**：
- **agent 评审报告必须逐项代码验证**：本评审 5 处 agent 误报/夸大（阶段 6 off-by-one、detectProto"缺 monitoring"、vite"缺 community-hub"、gate-engine"门禁失效"——实有 builtinGate、build-pipeline-new"会破坏"——实无调用点），均验证后甄别
- **自己引入的回归要自曝**：AST 死锁（清理删二进制）、check_memory_health 引号错误（体检从未真跑）——都是防回归测试暴露
- **每个修复都有行为验证**：注入测试 / 防回归 / 端到端，不靠读代码判断

## 遗留

- [ ] 阶段 6 归档在沙箱做不了文件移动 → 由 Owner 事后手动归档（已演示）
- [ ] resume 需手动传 ctx（SOP 已写，架构限制）
- [ ] 前端 web/pc 排序交互（Task 4.x）未实现（后端已完成）
- [ ] **L1 上下文预算超标**（实际 734 / 预算 400）——owner-agent + 项目编码规范待瘦身（已标注，未强行做）
- [ ] gate-engine verify 段保留为设计参考（未接线，由 verify-before-deliver skill + harness-checks 覆盖）
- [ ] dispatch 别名表 ↔ registry 一致性已纳入自检；新增服务时仍需跑 build-service-registry.sh + 补别名
