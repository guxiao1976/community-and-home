# 开发日志 — 2026-08-13

## 今日概览

- **涉及范围**：全局 harness 基础设施 + access-control 跨服务变更（7 服务）+ 变异测试质量提升
- **工作类型**：harness 流水线改造 + git 仓库治理 + access-control OpenSpec 全流程交付 + 流水线检验（暴露 8 项缺陷）+ 变异测试工具调研落地 + 核心文件变异提分
- **核心成果**：① harness 改造闭环；② access-control 7 服务交付（端限制/当前小区/配额/成员约束/同屋互见）；③ 流水线检验 8 项发现，能修尽修；④ 变异测试用 gomu 落地，核心 30 文件提到 80-100%

## 关键变更

### [Harness] 流水线改造（对齐 harness-design-principles.md，5 阶段 9 任务）

- **阶段1 文档重组**：`.harness/docs/` 43 文件 → 11 活跃 + `_archive/` 32 历史（`git mv` 保留历史，修 4 处活引用：quality-gates.yml、tools/new-service-with-tests.sh、change-directory-structure.md、README）
- **阶段2.1 服务名映射外提 registry**：消除 `harness-pipeline.js`/`core.js` 两份重复硬编码中文名，`serviceLabel()` 改读 `registry/services.json` 的 `displayName`
- **阶段2.2 项目策略标注**：`harness-checks.sh` 头部标注 Snowflake/错误码/跨服务为「项目策略」，新建 `.harness/config/project-policies.md`
- **阶段3.1 quality-gates 收敛**：8 阶段标注实现状态（6 `not-implemented` + 2 `partially-implemented`），gate-engine.js 注释对齐
- **阶段4.1 脚本归档**：归档 3 个无引用脚本；**发现 `build-pipeline.sh` 活跃**（被 pre-commit 依赖），从归档清单剔除
- **阶段5.1 三文档补写 Phase 12-17**：architecture/flow/patterns 补 dispatch 分级、确定性验证层、pipeline-review 检视、Incident/引擎策略分离模式

### [Harness] 2 个后续修复

- **gate-check 体系收敛**：归档 `harness-gate-check.sh`/`-v2.sh`（均无代码调用），修 owner-agent 等 4 处引用——真正门禁是 `gate-engine.js` validateGate + `harness-checks.sh`
- **displayName 生成 bug**：修 `generate-service-metadata.sh` 两个 bug——① CLAUDE.md 首标题 `# CLAUDE.md — xx` 提取出 "CLAUDE.md"（根因）；② `((generated++))` 在 `set -e` 下提前退出（历史 6 个 .service.json 分 3 次时间戳的元凶）。9 个服务 CLAUDE.md 首标题改为中文名

### [Git] 仓库治理（分支收敛 + 子模块 + 清理）

- **默认分支 master → main**：本地 + 远端 + GitHub 默认分支统一
- **删 4 个 stale 远端分支**：`main`(孤儿)、`001-identity-masterdata`、`worktree-masterdata`、`feature/web-admin`（均与 master 无共同祖先，成果已在 main）
- **`.gitmodules` 补 `services/ai-model-service` 条目**（之前有 gitlink 无登记）
- **3 个子仓库提交** CLAUDE.md 中文标题 + `.service.json`（ai-model/master-data/moderation）
- **清 44 个孤儿 worktree 分支** + `my-ralph-project` 取消跟踪（个人实验目录，gitignore）

### [Git] 治理沉淀为规范

- 新建 `.harness/rules/Git治理规范.md`（默认分支 main、分支命名、仓库命名、子模块治理），登记到 owner-agent L1 规则表
- `harness-checks.sh` 加 **check #17 git_hygiene**（gitlink↔.gitmodules 一致性 + 孤儿 worktree 分支检测），计数 16→17 项

### [access-control] OpenSpec 全流程交付（7 服务跨服务变更）

- **OpenSpec 链路**：需求分析（proposal + 5 spec + 4 CLAR 决策）→ 需求评审（2 轮 2/3 APPROVED）→ 架构设计（design + 31 tasks + 6 STAGE3 定稿，认证粒度 per-community 用户纠正）→ Proto 变更（4 文件兼容新增，api-proto `c86dc84`）→ N×Workflow 编码
- **7 服务交付**：permission（sys_role.platforms）、auth（端准入 50007）、user（当前小区/成员约束/同屋互见）、community-hub（板块配额 80007）、master-data（sys_section_quota）、web/mobile、web/pc
- **web/pc 历史债务清理**：122 条 TS 错误清零 + type-check 假通过修复（`web-pc-debt-cleanup`）
- **Stage 6 归档**：INDEX + 全链路编译 + summary

### [流水线检验] 真实 L 级变更暴露 8 项缺陷

- **RED 摘录无法机械强制**（第 4 次复发）→ 分诊（qa.js/generator.js：字段映射免 RED，有逻辑才要求）+ 回溯补录兜底
- **分诊滥用**（Generator 把逻辑函数误标「纯接线」）→ 变异测试才是正解
- **RED 标准不一致** → qa.js 统一「结构性不能替代真实摘录」
- **嵌套仓库检测盲区**（cross_service_import/proto_jstype 跳过 gitlink 内部）→ changed_files 扫子模块修复
- **type-check 假通过**（web/pc tsconfig files:[] 掩盖 122 错误）→ 已修
- **前端历史 build 债** → 已清（web-pc-debt-cleanup）
- **Generator 命名不匹配**（logic_test 约定被 pre-commit 拦截）→ 手工拆分修正

### [变异测试] gomu 落地 + 核心文件提分

- **工具调研**：`zimmski/go-mutesting` Go 1.25 崩溃、avito fork 极慢 → 改用 **gomu**（sivchari，Go≥1.24，增量/门禁/并行/JSON）
- **check #18 mutation_testing** 完整实现（gomu，diff 文件范围 + threshold 80 + 解析 score 判定）
- **方法论沉淀**：按「存活变异体 → 补分支测试」5 类共性（分支两端/返回 case/复合条件/转换边界/枚举映射）+ 等价变异边界（`==→<=`、`0→0`、fail-open `||→&&` 不追 80%）
- **核心 30 文件提分**：permission 11 文件（checkpermission 83.5%、createrole/revokerole/getrole 100%）、auth 5 文件（validatetoken 93.8%）、user 8 文件（join_community 81.8%、leave_community 40%→100%）、community-hub+master-data 5 文件（filter/validatescope 100%）
- **抓到真 bug**：invalidateusercache 正例测试没断言缓存被删（变异 `<=0→true` 存活）
- **踩坑**：并行子 Agent 同时跑 gomu 互相抢 CPU 全卡死 → 必须串行跑 gomu，子 Agent 只补测试

## 遗留问题

- [ ] `master-data`/`moderation` 仓库名与 module 名不一致（`community-masterdata` vs `community-master-data-service`、`community-moderation` vs `community-moderation-service`），收敛需改 go.mod import + 子模块 URL，单独排期
- [ ] 工程结构.md 隐含所有服务独立仓库，但实际仅 common + ai-model/master-data/moderation 是子模块，其余 6 服务跟踪在主仓库——结构差异待确认
- [ ] 变异测试 check #18 仍是 WARN（非阻塞）——等核心文件全覆盖后再考虑升阻塞
- [ ] ~180 个非核心 logic 文件未做变异提分（纯转换/展示类，价值低，按需做）
- [ ] access-control 的 web/pc 部分已交付但集成冒烟未跑（Stage 6 冒烟待做）

## 明日计划

- [ ] access-control Stage 6 补跑运行时冒烟
- [ ] （可选）剩余非核心文件按需变异提分
- [ ] （可选）评估 master-data/moderation 命名收敛方案
