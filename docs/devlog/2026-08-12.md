# 开发日志 — 2026-08-12

## 今日概览

- **涉及服务**：permission-service, master-data-service, user-service, community-hub-service, web/mobile
- **工作类型**：数据权限核心（access-data-permission）全 Wave 落地 + 跨服务集成验收 + 3 个服务功能开发
- **核心成果**：阶段①-⑥ 全部完成，T6.1 九项端到端验收矩阵全绿

## 关键变更

### [全局] 数据权限核心 Wave 2：阶段③ user-service 编排
- **做了什么**：
  - T3.1 CreateUser 自动分配 registered_user 基角色（DB 落库成功后 AssignRole(role_id=9, status=2)，失败仅告警不阻塞）
  - T3.2 JoinCommunity ownership（自有/租住）+ 自动授权 owner/tenant grant（community scope, status=0，未认证即可发布）
  - T3.3 LeaveCommunity 双调 RevokeRole 幂等撤销，失败恢复 bind_status
  - T3.4 门禁
- **为什么**：数据权限需要"有成员必有 scope、有注册必有基角色"
- **影响**：user-service 12 个新测试（gomock 真实 FAIL 摘录），16 PASS
- **QA/Review**：1 轮 PASS，Review 3/3 PASS（9 WARNING）

### [全局] 数据权限核心 Wave 2：阶段④ community-hub 消费方
- **做了什么**：
  - T4.0 JWT 身份注入通道（jwtUserID helper + PermMiddleware 顺序）
  - T4.1 publisher_id 取 JWT（覆盖客户端 body，防伪造）
  - T4.2-4.4 AssertPublishScope 挂载（lostfound/notice/contacts 写接口，错误码 060007→080006）
  - T4.5 moderation 回调以系统身份（user_id=0 global）reverse-lookup 校验
  - T4.6 读列表按 GetDataScopes 过滤（GLOBAL 不过滤 / LIMITED IN / EMPTY 空列表）
  - T4.7 错误码 080006 注册
- **修复（Review CRITICAL）**：GetNotice/GetLostFound 单条详情漏数据范围过滤；RPC 身份伪造（UserIDFromCtx 盲信 metadata + 0.0.0.0 绑定改回环）
- **影响**：3 轮迭代，16 PASS，41 测试，Review 3/3 PASS
- **踩坑**：QA FAIL 后修复需重跑刷新 _qa.md（已入记忆 `review-artifact-freshness`）

### [全局] 数据权限核心 Wave 2：阶段⑤ web/mobile 前端
- **做了什么**：T5.1 joinCommunity 携带 ownership（自有/租住必选表单）+ 楼/单元/房号输入
- **影响**：2 轮迭代（TDD 证据不足 → 补齐 vitest 真实 FAIL 摘录），17 测试全绿

### [全局] 数据权限核心 Wave 2：阶段⑥ 集成验收（Owner 内联）
- **做了什么**：全栈重建重启 + T6.1 九项端到端矩阵验证（注册用户发布❌ / 加入小区发布✅ / 未认证选举❌ / owner@A发B❌080006 / 伪造publisher_id❌ / 全局审核员✅ / 退出后缓存DEL❌ / 读列表scope过滤 / moderation回调放行拒绝）
- **修复 2 个真实缺陷**：
  1. **permission 种子缺口**：owner/tenant 缺发布+读权限、contact upsert 权限缺失、选举权限未绑定 → `init_permissions.sql` 新增 4.8/4.8.1/4.8.2
  2. **master-data scope 缓存 O(N²) 加载**：656k division OFFSET 分页需 27min 阻塞首次解析 → keyset 分页 + 启动预热 + 复合索引(migration 004)，实测 4 秒
- **影响**：所有服务 16 PASS；验收数据留库（测试用户 grant + notices）

### [Harness] 记忆系统新增 16 条建议落地
- **做了什么**：三个 Workflow 返回的 memorySuggestions 全部落地 `.harness/knowledge/memory/global/`（1 条 must-follow：`auto-grant-unverified-grant-confers-scope-level0`；15 条 should-follow），MEMORY.md 索引 + 倒排索引重建
- **影响**：后续开发自动加载相关经验

## 遗留问题

- [ ] moderation 读路径未按状态过滤（审核可见性门禁待后续 Wave，CHANGELOG 已注明）
- [ ] 选举功能（committee:election:vote）无 API 路由（权限已绑定 600，功能后续 Wave）
- [ ] 测试数据残留：验收创建的测试 grant（许3 sys_admin status=0 等）与 notices，如需可清理
- [ ] 5 个子模块内部未提交改动（my-ralph-project / ai-model / moderation / master-data / api-proto），需在子模块内各自提交

## 明日计划

- [ ] 提交 Wave 2 代码（根仓库 user/community-hub/web-mobile + 子模块 master-data/api-proto）
- [ ] 若需：清理验收测试数据、评估 moderation 读过滤
